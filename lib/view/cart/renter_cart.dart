import 'package:flutter/material.dart';

class RenteeCart extends StatelessWidget {
  const RenteeCart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Rentee Cart")),
    );
  }
}
